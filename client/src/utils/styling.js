const headerAndFooterDarkMode = {
     background: '#555',
     color: '#FEFEFA',
}

const headerAndFooterLightMode = {
     background: '#333',
     color: '#FEFEFA'
}

const generalDarkMode = {
     background: '#333',
     color: '#FEFEFA'
}

const generalLightMode = {
     background: '#FEFEFA',
     color: '#333'
}
const lightModeFontColor = {
     color: '#333'
}

const darkModeFontColor = {
     color: '#FEFEFA'
}

const darkModeBorder = {
     border: '1px #FEFEFA solid'
}

const lightModeBorder = {
     border: '1px #333 solid'
}

export {
     headerAndFooterDarkMode,
     headerAndFooterLightMode,
     generalDarkMode,
     generalLightMode,
     lightModeFontColor,
     darkModeFontColor,
     darkModeBorder,
     lightModeBorder
};